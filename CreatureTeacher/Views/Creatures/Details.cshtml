@{
	ViewData["Title"] = "Home Page";
}

@using CreatureTeacher.Models
@model CreatureTeacher.Models.Creature

@Html.DisplayFor(model => model.Name)'s Creature Features: 
<ul>
    <li>@Html.ActionLink($"{Model.Eye}", "Details", "Eyes", new { id = Model.EyeId })</li>
    <li>@Html.ActionLink($"{Model.Mouth}", "Details", "Mouth", new { id = Model.MouthId })</li>
    <li>@Html.ActionLink($"{Model.Head}", "Details", "Head", new { id = Model.HeadId })</li>
    <li>@Html.ActionLink($"{Model.Tail}", "Details", "Tail", new { id = Model.TailId })</li>
</ul>
@Html.ActionLink("Back to List", "Index")<div class="mdc-card demo-card demo-ui-control">

<div class="mdc-card demo-card">
  <div class="mdc-card__primary-action demo-card__primary-action" tabindex="0">
    <div class="mdc-card__media mdc-card__media--16-9 demo-card__media" style="background-image: url(&quot;https://material-components.github.io/material-components-web-catalog/static/media/photos/3x2/2.jpg&quot;);"></div>
    <div class="demo-card__primary">
      <h2 class="demo-card__title mdc-typography mdc-typography--headline6">@Html.DisplayFor(model => model.Name)</h2>
    </div>
    <div class="demo-card__secondary mdc-typography mdc-typography--body2">
        @Html.ActionLink($"{Model.Eye}", "Details", "Eyes", new { id = Model.EyeId })
        @Html.ActionLink($"{Model.Mouth}", "Details", "Mouth", new { id = Model.MouthId })
        @Html.ActionLink($"{Model.Head}", "Details", "Head", new { id = Model.HeadId })
        @Html.ActionLink($"{Model.Tail}", "Details", "Tail", new { id = Model.TailId })
    </div>
  </div>
  <div class="mdc-card__actions">
    <div class="mdc-card__action-buttons">
      <button class="mdc-button mdc-card__action mdc-card__action--button">  <span class="mdc-button__ripple"></span> Back to Creatures</button>
      <button class="mdc-button mdc-card__action mdc-card__action--button">  <span class="mdc-button__ripple"></span> Use as Parent</button>
    </div>
  </div>
</div>